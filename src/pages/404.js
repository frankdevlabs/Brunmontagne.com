import React from "react"; // eslint-disable-line no-unused-vars

const NotFoundPage = () => (
  <div>
    <p>Sorry, we cant find what you were looking for</p>
  </div>
);

export default NotFoundPage;
